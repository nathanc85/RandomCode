/* Remove focus blue highlight */
.k-autocomplete .k-input:focus,
.k-autocomplete.k-focus {
    box-shadow: none !important;
    border-color: transparent !important;
    outline: none !important;
}

/* Ensure the input blends naturally */
.k-autocomplete .k-input {
    outline: none !important;
    border: none !important;
    box-shadow: none !important;
    background-color: transparent !important;
}

/* Optional: If the span wrapper is causing issues */
.k-autocomplete {
    border: none !important;
    box-shadow: none !important;
    background: transparent !important;
}


/* Remove Bootstrap focus outline & blue glow */
.input-group .form-control:focus,
.input-group .form-control:focus-visible {
    box-shadow: none !important;
    border-color: transparent !important;
    outline: none !important;
}